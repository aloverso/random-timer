/* eslint-disable react-hooks/exhaustive-deps */

import React, { ReactElement } from "react";
import {RandomTimer} from "../src/RandomTimer";

const Index = (): ReactElement => {
  return <RandomTimer />;
};

export default Index;
